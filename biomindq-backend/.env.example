MONGODB_URI=mongodb://localhost:27017
GROQ_API_KEY=your_groq_api_key_here
NCBI_API_KEY=        # optional — PubMed works without it at a lower rate limit
DRUGBANK_API_KEY=    # optional — leave blank, feature stays inactive until filled
